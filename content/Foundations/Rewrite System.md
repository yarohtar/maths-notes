Fix $\Omega$, a finite set of symbols.
$(\Omega,P)$ is a rewrite system if $P$ is a finite set of [[Foundations/Rewrite Rule\|rewrite rules]]. 

### Proposition
If $\Omega$ is fixed, there are countably many rewrite systems on $\Omega$.
