A morphism $h:\mathcal{M}\to \mathcal{N}$ is an elementary $\mathcal{L}$-embedding 
if for any $\mathcal{L}$-formula $\varphi$ and $\bar{a}\in M$:
$$
\mathcal{M}\models \varphi(\bar{a}) \iff \mathcal{N}\models \phi(h(\bar{a}))
$$
