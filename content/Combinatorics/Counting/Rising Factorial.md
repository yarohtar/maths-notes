Given $k\in \mathbb{N}$, define rising factorial
$$
x^{\overline{k}} = x(x+1)\dots(x+k-1)
$$
Note that for [[Combinatorics/Counting/Falling Factorial]] we have:
$$
(-x)^{\underline{ k }} = (-1)^{k} x^{\overline{ k }}
$$
